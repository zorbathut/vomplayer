<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <IsBuildingOnLinux>$([MSBuild]::IsOSPlatform('Linux'))</IsBuildingOnLinux>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
    <PackageReference Include="GirCore.Gtk-4.0" Version="0.7.0" />
  </ItemGroup>

  <Target Name="BuildHdrHelper" Condition="'$(IsBuildingOnLinux)' == 'true'" BeforeTargets="Build"
          Inputs="$(MSBuildProjectDirectory)/../../native/hdr_helper.c;$(MSBuildProjectDirectory)/../../native/color-management-v1-protocol.c;$(MSBuildProjectDirectory)/../../native/color-management-v1-client-protocol.h;$(MSBuildProjectDirectory)/../../native/presentation-time-protocol.c;$(MSBuildProjectDirectory)/../../native/presentation-time-client-protocol.h"
          Outputs="$(OutDir)libhdr_helper.so">
    <Exec Command="gcc -shared -fPIC -O2 -Wall -o $(OutDir)libhdr_helper.so $(MSBuildProjectDirectory)/../../native/hdr_helper.c $(MSBuildProjectDirectory)/../../native/color-management-v1-protocol.c $(MSBuildProjectDirectory)/../../native/presentation-time-protocol.c -lwayland-client -lwayland-egl -lEGL -lm" />
  </Target>

  <ItemGroup Condition="'$(IsBuildingOnLinux)' == 'true'">
    <Content Include="$(OutDir)libhdr_helper.so" CopyToPublishDirectory="PreserveNewest" />
  </ItemGroup>
</Project>
